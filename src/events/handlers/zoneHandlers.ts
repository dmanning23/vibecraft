/**
 * Zone Status Event Handlers
 *
 * In village mode, zone/location status is handled by React via VillageContext.
 * This file is a no-op placeholder for compatibility.
 */

/**
 * Register zone status event handlers
 * In village mode, zone status is handled by React's VillageContext.
 */
export function registerZoneHandlers(): void {
  // In village mode, zone status is handled by React via VillageContext.
  // See src/state/VillageContext.tsx and src/hooks/useEventSubscription.ts
}
