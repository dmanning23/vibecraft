/**
 * Animation Handlers
 *
 * In village mode, character animations are handled via CSS in the React components.
 * This file is a no-op placeholder for compatibility.
 */

/**
 * Register animation event handlers
 * In village mode, animations are handled by React CSS animations.
 */
export function registerAnimationHandlers(): void {
  // In village mode, character animations are CSS-based in ClaudeCharacter.tsx
  // See src/components/Character/ClaudeCharacter.tsx and ClaudeCharacter.css
}
